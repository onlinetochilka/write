# Дизайн-система приложения «Ежедневник репетитора» (Точилка)

Подробный справочник по актуальному UI Kit и дизайн-системе проекта, составленный на основе кодовой базы (Tailwind-конфигурации, корневых стилей и компонентов).

## 1. Цветовая палитра

В приложении используется премиальный, спокойный и современный цветовой профиль (Neumorphism / Glassmorphism стиль), адаптированный для повседневной работы преподавателей.

### Базовые цвета
- **Фон приложения (Ivory):** `#EDF2F7` (`rgb(237 242 247)`) — жемчужно-серый оттенок, на котором выделяются карточки.
- **Фон редактора программ:** `#FDFBF7` — теплый кремовый оттенок.
- **Фон карточек:** Белый `#FFFFFF` (elevated) или полупрозрачный белый `rgba(255, 255, 255, 0.70)` для Glass-карточек.
- **Цвета текста (Stone Palette):**
  - Обычный текст: `stone-800` (`#292524`) / `stone-700` (`#44403C`)
  - Приглушенный текст / подписи: `stone-500` (`#78716C`) / `stone-400` (`#A8A29E`)
  - Акцентный (заголовки): `stone-900` (`#1C1917`)

### Акцентные цвета
- **Основной бренд-цвет (Главные кнопки, акценты, рамка фокуса):** `brand-blue` (`#006584`) — глубокий синий.
- **Второстепенный бренд-цвет:** `brand-burgundy` (`#B71234`) — насыщенный бордовый.
- **Академический синий (Для бокового меню и заголовков секций):** `#1B4F72` / `#1B2A47`.
- **Иконки (неактивные):** `slate-400` (`#94A3B8`) / `slate-500` (`#64748B`).

### Пастельные цвета (Статусы и Уроки)
Для динамических сущностей (ученики, программы) применяется сгенерированная палитра в цветовом пространстве OKLCH (Chroma: 0.12, Lightness: 0.89-0.95), обеспечивающая уникальные пастельные тона.
Для жестко заданных бейджей статусов используются стандартные классы Tailwind:
- **Голубой (Primary / Инфо):** Фон `indigo-100` (`#E0E7FF`), Текст `indigo-800` (`#3730A3`).
- **Зеленый (Success / Успех):** Фон `emerald-100` (`#D1FAE5`), Текст `emerald-800` (`#065F46`).
- **Желтый (Warning / Ожидание):** Фон `amber-100` (`#FEF3C7`), Текст `amber-800` (`#92400E`).
- **Розовый/Красный (Danger / Отмена):** Фон `red-100` (`#FEE2E2`), Текст `red-800` (`#991B1B`).
- **Фиолетовый (Subject / Предметы):** Фон `violet-100` (`#EDE9FE`), Текст `violet-800` (`#5B21B6`).

Цвета разделов в боковом меню (Sidebar):
- Главная: `#3B5266` (Slate Blue)
- Расписание: `#1B4F72` (Academic Blue)
- Ученики: `#7A404D` (Muted Terracotta / Plum)
- Программы: `#7A5299` / `#735B7A` (Muted Plum)
- Финансы: `#426B5C` (Deep Sage / Viridian)
- Настройки: `#636B74` (Cool Graphite)

## 2. Типографика

- **Семейство шрифтов:** `"Plus Jakarta Sans", system-ui, sans-serif` (с активированными фичами `"cv02", "cv03", "cv04", "cv11"`).
- **Базовый интерлиньяж (line-height):** `1.5`
- **Заголовки карточек (`Card.Title`):**
  - Размер: `text-base` (16px)
  - Насыщенность: `font-semibold` (600)
  - Цвет: `text-stone-900`
  - Межстрочный интервал: `leading-snug` (1.375)
- **Основной текст:**
  - Размер: обычно `text-sm` (14px)
  - Насыщенность: `font-normal` (400) или `font-medium` (500) для навигации.
- **Подписи и бейджи:** `text-xs` (12px), `font-medium` (500).

## 3. Стилизация элементов

Все анимации интерфейса опираются на функцию плавности: `ease-out-quart` (`cubic-bezier(0.25, 1, 0.5, 1)`). Фокусные элементы имеют обводку (Focus Ring) цвета `#006584`.

### Карточки (`Card`)
Основной элемент группировки контента. Имеет три варианта:
1. **Elevated (По умолчанию):**
   - Фон: `bg-white`
   - Скругления: `rounded-2xl` (16px)
   - Обводка/Тень: `shadow-sm`, `ring-1 ring-slate-200`
2. **Flat:**
   - Фон: `bg-ivory`
   - Обводка: `border border-stone-200/30`
3. **Glass (Неоморфизм/Стекло):**
   - Фон: Полупрозрачный белый `rgb(255 255 255 / 0.70)`
   - Эффект: `backdrop-blur(12px)`
   - Рамка: `border 1px solid rgb(255 255 255 / 0.50)`
- **Внутренние отступы:** `p-5` (20px) по умолчанию.
- **Hover-эффект (опционально):** `hover:shadow-md hover:-translate-y-1` с плавностью 300ms.

### Кнопки (`Button`)
Имеют легкий эффект вдавливания при клике (`active:scale-[0.98]`).
- **Скругления и отступы:**
  - `sm`: Высота `h-8`, отступы `px-3`, скругление `rounded-lg` (8px).
  - `md` (По умолчанию): Высота `h-10`, отступы `px-4`, скругление `rounded-xl` (12px).
  - `lg`: Высота `h-12`, отступы `px-6`, скругление `rounded-xl`.
- **Состояния (Вариант Filled):**
  - **Base:** `bg-brand-blue` (`#006584`), `text-white`, `font-semibold`.
  - **Тени:** `shadow-[0_4px_14px_rgba(0,101,132,0.30)]`.
  - **Hover:** Изменение цвета на `#005270`, легкое поднятие `-translate-y-0.5`, усиленная тень.
  - **Active:** Цвет `#004560`, возвращение на нулевую позицию `translate-y-0`.
- **Состояния (Вариант Outline / Secondary):**
  - **Base:** `bg-white`, обводка `border-stone-200`, текст `stone-600` / `stone-700`.
  - **Hover:** `bg-stone-50`, текст `stone-800`.
  - **Active:** `bg-stone-100`.

## 4. Специфические правила

### Правило дизайна персонажей (Аватары)
**Строгое ограничение:** Если в приложении используются персонажи (в качестве пресетов аватаров), они **обязательно должны оставаться в форме деревянных карандашей**. Использование аватаров с человеческими лицами недопустимо! 
Если у пользователя не загружен аватар и не выбран пресет, используется квадратная градиентная монограмма с первой буквой имени.
